syntax = "proto3";

package chatpackage;
option go_package = "https://github.com/Troelshjarne/mini_project_2;chatpackage";

message chatMessage {
string message = 1;
repeated int32 vectorTime = 2;
}



service Publish{
   rpc getMessage (stream chatMessage) returns (stream chatMessage);
}